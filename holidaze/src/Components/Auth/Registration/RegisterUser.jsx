import UserToRegister from "./UserToRegister";

const RegisterUser = () => {
    // const { data, isLoading, isError } = usePostApi(URL + register, body);
    // console.log(data);
    // Create userToRegister input
    // validation
    // errorhandling
    return (
        <>
        <h1>REGISTRATION</h1>
        <UserToRegister />
        </>
    )
};

export default RegisterUser;